project restructure: it's little messy atm. i think we should move most of the .md files to a new folder, not `docs`, as that should be for actual documentation. maybe `notes` or something? idk. suggestion something. also, maybe create a `src` folder for the actual code, and move most of the files there. keep the root for github specific files, etc.

this: `"args": ["/path/to/your/project"]` needs to go.
 MCP should be configurable globally, not project specific.
 maybe have project parameter on the tools instead? maybe both/hybrid? idk.


search_tool: enhance with syntax-tree-based semantic filtering.
  or maybe have a separate tool for that? like "semantic_search" or something.
  
do we really need the DI stuff (`builder.Services.AddTransient<MyNewTool>()`? i think the C# MCP SDK docs say otherwise, but i can't find that now.

is there a roslyn way to `dotnet build` (`clean`/`restore`) without `dotnet`?


